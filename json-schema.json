{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JestGen configuration",
  "type": "object",
  "properties": {
    "useTemplate": {
      "description": "Whether to use a template for the test files.",
      "type": "boolean"
    },
    "templatePath": {
      "description": "The path to the template file, if useTemplate is true.",
      "type": "string"
    },
    "useSupertest": {
      "description": "Whether to use Supertest in the test files.",
      "type": "boolean"
    },
    "appPath": {
      "description": "The path to the app file, if useSupertest is true. This is required if useSupertest is true.",
      "type": "string"
    }
  },
  "required": ["useTemplate", "useSupertest"]
}
